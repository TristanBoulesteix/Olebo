name: Create Olebo distributable

on:
  push:
    branches: [ Github-action ]
  pull_request:
    branches: [ Github-action ]

jobs:
  build:

    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 16
        uses: actions/setup-java@v2
        with:
          java-version: '16'
          distribution: 'adopt'
          cache: gradle
      - name: Validate Gradle wrapper
        uses: gradle/wrapper-validation-action@e6e38bacfdf1a337459f332974bb2327a31aaf4b
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Install Chocolatey
        run: Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
      - name: Install Inno Setup
        run: 'choco install innosetup'
      - name: Create distributable
        run: './gradlew Olebo:createDistributable'
      - name: Create setup
        run: 'iscc Installer\OleboSetup.iss'
      - name: Test
        run: New-Item -Path . -Name "test.txt" -ItemType "file" -Value "This is a text string."
      - uses: EndBug/add-and-commit@v7
        with:
          add: .
          author_name: Github action
          author_email: tristan.boulesteix@gmail.com

